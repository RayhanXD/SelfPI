# Leave empty (or unset) for local `npm run dev` — Vite proxies /apis, /auth, etc. to :8000.
# Production (Vercel): absolute origin of the Docker-hosted API, no trailing slash.
# VITE_API_URL=https://api.example.com
VITE_API_URL=
