# Frontend lab

**Primary UI surface for design work:** [http://localhost:5180](http://localhost:5180)

Mock-data product prototype (Dashboard, Inbox, Watched APIs, Settings, Change Detail). Design system: [`../frontend/design/`](../frontend/design/README.md).

Do not use `:5173` for design/UX iteration unless explicitly asked — that is the API-wired production app.

```bash
npm install
npm run dev
```
